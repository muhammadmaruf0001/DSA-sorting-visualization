{
  "name": "algorithm-visualizer",
  "version": "1.0.0",
  "description": "Algorithm Visualizer project with local server setup",
  "scripts": {
    "start": "http://localhost:8000/index.html{
  "name": "algorithm-visualizer",
  "version": "1.0.0",
  "description": "Algorithm Visualizer project with local server setup",
  "scripts": {
    "start": "live-server --port=8000",
    "build": "echo 'Building...' && echo 'Built!'"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
